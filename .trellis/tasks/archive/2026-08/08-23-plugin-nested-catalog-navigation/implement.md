# 实施计划

1. 扩展 Server 分类查询：分类摘要、`category` 过滤、权限和顺序测试。
2. 扩展插件 Manifest 与媒体契约，兼容 v1/v2 导航并实现节点 token。
3. 扩展在线媒体 handler/service/runtime 和错误日志。
4. 扩展 Player 在线 ID、解析器和 ServerDataSource 列表分派。
5. 调整 SourceLibraryView 的 Server 根页与多层空态/错误态。
6. 更新 Bilibili 插件为层级模式并加入番剧地区节点。
7. 做标准库、Bilibili、多层、旧插件兼容和恶意响应的跨层测试。
