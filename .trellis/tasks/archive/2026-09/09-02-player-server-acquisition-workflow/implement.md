# Implementation Plan

1. 扩展 Player Server DTO：结果页码、入库进度、默认/可选目标、账号任务列表。
2. 增加站点选择器、结果工作区、入库三步确认组件。
3. 重构详情页，去掉平铺配置和本地返回按钮，接入流式页签、分页及状态刷新。
4. 增加搜索来源返回上下文，返回时恢复全局搜索工作区。
5. 增加入库任务 Pinia UI 状态和右侧悬浮入口/任务面板。
6. 在 Server 媒体库中增加“正在入库”虚拟分类、占位海报与安全详情状态。
7. 补充针对结果流、独立分页、默认选择、返回、安全请求路径及占位分类的验证脚本断言。
8. 执行 typecheck、lint、build、verify:server-datasource；必要时修复发现。
